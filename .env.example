# PostgreSQL connection (used by docker-compose and the API)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=code_connect
POSTGRES_PASSWORD=code_connect
POSTGRES_DB=code_connect

# Auth
JWT_SECRET=change-me-in-production
